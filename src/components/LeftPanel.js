import React from 'react'

// Components
import Logo from './Logo'

export default () => {
    return (
        <div id="left-panel" className="panels dark">
            <div className="main-logo-container">
                <Logo />
            </div>
        </div>
    );
}